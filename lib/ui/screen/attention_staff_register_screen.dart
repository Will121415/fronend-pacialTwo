import 'package:flutter/material.dart';

class AttentionStaffRegister extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      alignment: Alignment.center,
      child: Center(
        child: Text('Register Attention Staff'),
      ),
    );
  }
}
